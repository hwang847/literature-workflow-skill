interface:
  display_name: "Literature Workflow"
  short_description: "Set up, read, and note papers"
  default_prompt: "Use $literature-workflow to set up this literature workspace."
policy:
  allow_implicit_invocation: true
