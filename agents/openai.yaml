interface:
  display_name: "Literature Workflow"
  short_description: "Read papers and manage sources"
  default_prompt: "Use $literature-workflow to read a local paper and explain its pipeline."
policy:
  allow_implicit_invocation: true
